
# ** updated single, clean architecture diagram that now explicitly includes the sim/ folder (waves + logs) **
```
┌────────────────────────────── UVM TESTBENCH ──────────────────────────────┐
│  Sequences → Driver → DUT → Monitor → Scoreboard → Coverage               │
│  (BER stress, retrain)     │   │   (BER, lock time, deskew, LTSSM states) │
└─────────────────────────────┴─────────────────────────────────────────────┘
        │                               │
        │                               └──────────────▶ sim/logs/run.log
        │
        ▼
┌─────────────── gpu_serdes_system (rtl/top) ───────────────┐
│                                                           │
│  ┌──────────── NVLINK FRAMING (rtl/protocol) ─────────────┐
│  │ Payload + Coherence + Sequence ID                      │
│  └───────────────┬───────────────────────────────────────┘│
│                  │ 128-bit flit                           │
│  ┌───────────────▼───────────────────────────────────────┐│
│  │               PCS LAYER (rtl/pcs)                     ││
│  │ Scrambler → 128b/130b Encode → Running Disparity       │
│  └───────────────┬───────────────────────────────────────┘│
│                  │ 130-bit                                │
│  ┌───────────────▼───────────────────────────────────────┐│
│  │               PHY LAYER (rtl/phy)                     ││
│  │ TX Pipeline → BER → Jitter → CDR → RX Pipeline + Deskew│
│  └───────────────┬───────────────────────────────────────┘│
│                  │ 1-bit serial stream                    │
└──────────────────┼────────────────────────────────────────┘
                   │
        ┌──────────────────── CHANNEL MODEL ────────────────────┐
        │ BER + Random Jitter (RJ) + Deterministic Jitter (DJ)  │
        └────────────────────────┬──────────────────────────────┘
                                 │
┌──────────────────▲────────────────────────────────────────┐
│               PHY LAYER (rtl/phy)                         │
│ RX Pipeline + CDR Lock + Deskew                           │
└──────────────────▲────────────────────────────────────────┘
                   │ 130-bit
┌──────────────────▲────────────────────────────────────────┐
│               PCS LAYER (rtl/pcs)                         │
│ 128b/130b Decode → RD Check → Descrambler                 │
└──────────────────▲────────────────────────────────────────┘
                   │ 128-bit
┌──────────────────▲─────────────────────────────────────────┐
│        NVLINK DECODE (rtl/protocol)                        │
│ Extract Payload, Coherence, Seq Check                      │
└────────────────────────────────────────────────────────────┘

        ┌────────────── PCIe LTSSM (rtl/protocol) ──────────────┐
        │ DETECT → POLLING → CONFIG → L0 → L0s/L1/L2/L3         │
        │                ↘ RECOVERY / RETRAIN                   │
        └───────────────────────────────────────────────────────┘

        ┌────────────── EYE ANALYSIS (rtl/analysis) ───────────┐
        │ Sample counter + eye-opening ratio                   │
        └──────────────────────────────────────────────────────┘

        ┌────────────────────── SIM OUTPUTS ────────────────────┐
        │ sim/waves/serdes.vcd   ←───────────── Waveforms       │
        │ sim/logs/run.log       ←───────────── Test results    │
        └───────────────────────────────────────────────────────┘
```

---

## 📂 **How the `sim/` folder maps in this diagram**

| Diagram element                 | Your repo path         |
| ------------------------------- | ---------------------- |
| Waveform capture from DUT       | `sim/waves/serdes.vcd` |
| Test status / scoreboard prints | `sim/logs/run.log`     |
| Stimulus source                 | `tb/sequences/`        |
| Verification control            | `tb/uvm/`              |
| DUT                             | `rtl/top/`             |
| PHY models (BER/Jitter/CDR)     | `rtl/phy/`             |
| PCS (128b/130b + scrambler)     | `rtl/pcs/`             |
| LTSSM + NVLink framing          | `rtl/protocol/`        |
| Eye statistics                  | `rtl/analysis/`        |

---

